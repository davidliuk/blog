import{_ as e,V as a,W as n,Z as i}from"./framework-e5d7a6b2.js";const s={},d=i(`<h1 id="go" tabindex="-1"><a class="header-anchor" href="#go" aria-hidden="true">#</a> Go</h1><h2 id="golang基本语法" tabindex="-1"><a class="header-anchor" href="#golang基本语法" aria-hidden="true">#</a> Golang基本语法</h2><h3 id="数据类型" tabindex="-1"><a class="header-anchor" href="#数据类型" aria-hidden="true">#</a> 数据类型</h3><p>Golang中的数据类型包括基本数据类型和复合数据类型。</p><h4 id="基本数据类型" tabindex="-1"><a class="header-anchor" href="#基本数据类型" aria-hidden="true">#</a> 基本数据类型</h4><p>Golang中的基本数据类型包括：</p><ul><li>bool：布尔类型，取值为true或false。</li><li>数值类型：包括整数类型和浮点数类型。 <ul><li>整数类型：int8、int16、int32、int64、uint8、uint16、uint32、uint64、int、uint、uintptr。</li><li>浮点数类型：float32、float64。</li></ul></li><li>字符串类型：用于表示文本数据。</li><li>字符类型：用于表示单个字符，使用rune类型表示。</li><li>错误类型：用于表示错误信息，通常用于函数返回值。</li></ul><h4 id="复合数据类型" tabindex="-1"><a class="header-anchor" href="#复合数据类型" aria-hidden="true">#</a> 复合数据类型</h4><p>Golang中的复合数据类型包括：</p><ul><li>数组类型：用于表示一组固定长度的数据。</li><li>切片类型：用于表示一组长度可变的数据。</li><li>映射类型：用于表示一组键值对的数据。</li><li>结构体类型：用于表示一组相关的数据。</li><li>接口类型：用于表示一组方法的集合。</li></ul><h3 id="流程控制" tabindex="-1"><a class="header-anchor" href="#流程控制" aria-hidden="true">#</a> 流程控制</h3><p>Golang中的流程控制语句包括：</p><ul><li>条件语句：if语句、switch语句。</li><li>循环语句：for语句。</li><li>跳转语句：break语句、continue语句、goto语句。</li></ul><h4 id="条件语句" tabindex="-1"><a class="header-anchor" href="#条件语句" aria-hidden="true">#</a> 条件语句</h4><p>if语句用于根据条件执行不同的代码块，语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code><span class="token keyword">if</span> condition <span class="token punctuation">{</span>
    <span class="token comment">// code block</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token keyword">if</span> condition <span class="token punctuation">{</span>
    <span class="token comment">// code block</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
    <span class="token comment">// code block</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>switch语句用于根据不同的条件执行不同的代码块，语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code><span class="token keyword">switch</span> expression <span class="token punctuation">{</span>
<span class="token keyword">case</span> value1<span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token keyword">case</span> value2<span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token keyword">default</span><span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h4 id="循环语句" tabindex="-1"><a class="header-anchor" href="#循环语句" aria-hidden="true">#</a> 循环语句</h4><p>for语句用于循环执行一段代码，语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>for initialization; condition; post {
    // code block
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>其中，initialization用于初始化循环变量，condition用于判断循环是否继续，post用于更新循环变量。</p><h4 id="跳转语句" tabindex="-1"><a class="header-anchor" href="#跳转语句" aria-hidden="true">#</a> 跳转语句</h4><p>break语句用于跳出循环，continue语句用于跳过当前循环，goto语句用于无条件跳转到指定的标签处。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>for i := 0; i &lt; 10; i++ {
    if i == 5 {
        break
    }
    if i == 3 {
        continue
    }
    fmt.Println(i)
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>goto label
// code block
label:
// code block
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>总的来说，Golang中的流程控制语句包括条件语句、循环语句和跳转语句，可以根据实际情况选择合适的语句进行控制程序的流程。</p><h3 id="变量声明" tabindex="-1"><a class="header-anchor" href="#变量声明" aria-hidden="true">#</a> 变量声明</h3><p>在Golang中，变量声明可以使用var关键字或短变量声明（:=）。</p><p>使用var关键字声明变量的语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var variable_name data_type
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，variable_name为变量名，data_type为变量的数据类型。</p><p>例如，声明一个整数变量x：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var x int
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>可以在声明变量的同时进行初始化，语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var variable_name data_type = initial_value
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>例如，声明一个整数变量x并初始化为10：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var x int = 10
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>使用短变量声明（:=）可以更简洁地声明变量并进行初始化，语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>variable_name := initial_value
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>例如，声明一个整数变量x并初始化为10：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>x := 10
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>需要注意的是，短变量声明只能用于函数内部，而var关键字可以用于函数内部和外部。另外，Golang中的变量声明必须使用，否则会编译错误。</p><h2 id="并发编程" tabindex="-1"><a class="header-anchor" href="#并发编程" aria-hidden="true">#</a> 并发编程</h2><p>Golang是一门天生支持并发编程的语言，它提供了丰富的并发编程机制，包括goroutine、channel、select等。以下是Golang并发编程的一些基本概念和使用方法：</p><h3 id="goroutine" tabindex="-1"><a class="header-anchor" href="#goroutine" aria-hidden="true">#</a> Goroutine</h3><p>Goroutine是Golang中的轻量级线程，可以在一个程序中同时运行多个Goroutine，每个Goroutine都是独立的执行流程。Goroutine的创建和启动非常简单，只需要在函数调用前加上go关键字即可，例如：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>go func() {
    // code block
}()
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="channel" tabindex="-1"><a class="header-anchor" href="#channel" aria-hidden="true">#</a> Channel</h3><p>Channel是Golang中的一种通信机制，用于在Goroutine之间传递数据。Channel有两种类型：带缓冲的Channel和不带缓冲的Channel。带缓冲的Channel可以缓存一定数量的数据，当缓存满时，发送方会被阻塞，直到接收方取走数据。不带缓冲的Channel在发送和接收数据时都会被阻塞，直到有对应的接收或发送操作为止。</p><p>创建Channel的语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code>ch <span class="token operator">:=</span> <span class="token function">make</span><span class="token punctuation">(</span><span class="token keyword">chan</span> data_type<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>发送数据到Channel的语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code>ch <span class="token operator">&lt;-</span> data
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>从Channel接收数据的语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code>data <span class="token operator">:=</span> <span class="token operator">&lt;-</span>ch
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="select" tabindex="-1"><a class="header-anchor" href="#select" aria-hidden="true">#</a> Select</h3><p>Select语句用于在多个Channel之间进行选择，当有一个Channel可以进行发送或接收操作时，就会执行对应的代码块。Select语句可以用于实现超时、非阻塞等功能。</p><p>Select语句的语法如下：</p><div class="language-go line-numbers-mode" data-ext="go"><pre class="language-go"><code><span class="token keyword">select</span> <span class="token punctuation">{</span>
<span class="token keyword">case</span> data <span class="token operator">:=</span> <span class="token operator">&lt;-</span>ch1<span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token keyword">case</span> data <span class="token operator">:=</span> <span class="token operator">&lt;-</span>ch2<span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token keyword">default</span><span class="token punctuation">:</span>
    <span class="token comment">// code block</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>以上是Golang并发编程的一些基本概念和使用方法，需要注意的是，在并发编程中需要注意线程安全和数据竞争等问题，可以使用锁、原子操作等机制来解决这些问题。</p><h3 id="对象声明" tabindex="-1"><a class="header-anchor" href="#对象声明" aria-hidden="true">#</a> 对象声明</h3><p>在Golang中，new和make都是用于创建新的数据结构的内置函数，但是它们的作用不同。</p><p>new用于创建一个指定类型的指针，并返回该指针的地址。new的语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ptr := new(data_type)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，data_type为指定的数据类型，ptr为指向该数据类型的指针。</p><p>例如，创建一个整数类型的指针：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ptr := new(int)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>make用于创建一个指定类型的数据结构，并返回该数据结构的引用。make只能用于创建slice、map和channel等数据结构，因为这些数据结构需要进行初始化才能使用。make的语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ref := make(data_type, length, capacity)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，data_type为指定的数据类型，length为数据结构的长度，capacity为数据结构的容量（仅适用于slice和channel）。</p><p>例如，创建一个长度为10、容量为20的整数类型的slice：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>s := make([]int, 10, 20)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>需要注意的是，new和make都是用于创建新的数据结构，但是它们的作用不同。new用于创建指针类型的数据结构，而make用于创建slice、map和channel等数据结构。在使用new和make时，需要根据实际情况选择合适的函数，并进行适当的初始化。</p><p>在Golang中，数组和切片都是用于存储一组数据的数据结构，但是它们之间有很大的区别。</p><h2 id="数组与切片" tabindex="-1"><a class="header-anchor" href="#数组与切片" aria-hidden="true">#</a> 数组与切片</h2><h3 id="数组" tabindex="-1"><a class="header-anchor" href="#数组" aria-hidden="true">#</a> 数组</h3><p>数组是一组固定长度的数据结构，数组的长度在创建时就已经确定，不能动态改变。数组的元素类型可以是任意类型，但是所有元素的类型必须相同。数组的声明和初始化语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var array_name [length]data_type
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，array_name为数组名，length为数组长度，data_type为数组元素类型。</p><p>例如，创建一个长度为5的整数类型数组：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var a [5]int
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="切片" tabindex="-1"><a class="header-anchor" href="#切片" aria-hidden="true">#</a> 切片</h3><p>切片是一个动态长度的数据结构，可以根据需要动态增加或减少长度。切片的元素类型和数组类似，但是切片的长度可以动态改变。切片的声明和初始化语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var slice_name []data_type
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，slice_name为切片名，data_type为切片元素类型。</p><p>例如，创建一个整数类型的切片：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>var s []int
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>切片可以通过make函数进行初始化，语法如下：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>slice_name := make([]data_type, length, capacity)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，length为切片的长度，capacity为切片的容量。</p><p>例如，创建一个长度为5、容量为10的整数类型切片：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>s := make([]int, 5, 10)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="区别" tabindex="-1"><a class="header-anchor" href="#区别" aria-hidden="true">#</a> 区别</h3><p>数组和切片之间的主要区别如下：</p><ol><li><p>长度：数组的长度在创建时就已经确定，不能动态改变，而切片的长度可以动态改变。</p></li><li><p>内存分配：数组在创建时就会分配一段连续的内存空间，而切片在创建时不会分配内存空间，只有在添加元素时才会动态分配内存空间。</p></li><li><p>传递方式：数组在函数调用时会进行值传递，即会复制一份数组，而切片在函数调用时会进行引用传递，即会传递切片的指针。</p></li></ol><p>总的来说，数组和切片都是用于存储一组数据的数据结构，但是它们之间的区别很大，需要根据实际情况选择合适的数据结构。</p>`,97),l=[d];function t(c,r){return a(),n("div",null,l)}const p=e(s,[["render",t],["__file","语法.html.vue"]]);export{p as default};
