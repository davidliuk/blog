Select all correct statements

You have responded 4 times. Each option may only be selected once.

- In asynchronous systems, flooding guarantees a request will reach all nodes in the network if there is no network partition

- **In iterative DNS naming resolution, the root servers retain no state of the client requests**
- Anycast DNS guarantees globally consistent answers

- 
  DNSSEC can defend against DDoS attacks

- 
  In P2P network, a new node can join the system without knowledge of an existing node in the network

- 
  DNSSEC provides authenticity but not confidentiality.

- In Akamai, small TTL is used to improve latency





You have responded 2 times. Each option may only be selected once.

- Total ordering implies FIFO ordering but not causal
- FIFO ordering implies causal ordering (your response)
- **Causal ordering implies FIFO ordering**
- If a sender multicasts m1 before m2, total ordering guarantees m1 is delivered before m2