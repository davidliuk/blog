# BFS 进阶

> 性价比之王，14, 47

构图具有一定的灵活性，很多题一开始看起来不是图，但只要状态之间能连边，就能转成 BFS。

## 高频提醒

- 连通块、层级遍历、拓扑排序都优先想 BFS
- 一旦入队就要立刻标记，避免重复入队
- 不需要距离时，`visited` 集合就够；需要距离时，用 `distance` 哈希表

## 进阶点

- 图上的 BFS
- 分层 vs 不分层
- 双向 BFS
- 拓扑排序

建议配合 `11. BFS` 一起看：前者是基础，后者是进阶与建模。
